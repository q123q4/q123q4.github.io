# 个人主页项目

一个使用 Next.js (App Router) 和 Tailwind CSS 构建的现代化个人主页/作品集网站。

## 功能特性

- ✨ 响应式设计，移动优先
- 🎨 现代化的 UI 设计，支持动画效果
- 📱 完全响应式，适配各种设备
- ♿ 良好的可访问性支持
- 🔍 SEO 优化
- ⚡ 基于 Next.js 14 App Router，性能优异

## 技术栈

- **框架**: Next.js 14 (App Router)
- **样式**: Tailwind CSS
- **语言**: TypeScript
- **部署**: Vercel

## 快速开始

### 安装依赖

```bash
npm install
```

### 开发模式

```bash
npm run dev
```

在浏览器中打开 [http://localhost:3000](http://localhost:3000) 查看效果。

### 构建生产版本

```bash
npm run build
```

### 启动生产服务器

```bash
npm start
```

### 代码检查

```bash
npm run lint
```

## 项目结构

```
.
├── app/                    # Next.js App Router
│   ├── layout.tsx         # 根布局
│   ├── page.tsx           # 首页
│   └── globals.css        # 全局样式
├── components/            # React 组件
│   ├── Navbar.tsx         # 导航栏
│   ├── Hero.tsx           # 首页 Hero 区域
│   ├── About.tsx          # 关于我
│   ├── Projects.tsx        # 项目展示
│   ├── Contact.tsx        # 联系方式
│   └── Footer.tsx         # 页脚
├── content/              # 内容数据
│   └── data.json         # 个人数据（可编辑）
└── public/               # 静态资源
    └── avatar.jpg        # 头像图片
```

## 自定义内容

### 1. 更新个人信息

编辑 `content/data.json` 文件，替换为你的真实信息：

- **hero**: 姓名、标题、描述、头像路径
- **about**: 个人简介、技能、工作经历
- **projects**: 项目列表（标题、描述、技术栈、链接）
- **contact**: 联系方式（邮箱、GitHub、LinkedIn 等）

### 2. 添加头像

将你的头像图片放到 `public/avatar.jpg`（或修改 `data.json` 中的路径）。

### 3. 添加项目图片

将项目图片放到 `public/` 目录，并在 `data.json` 中引用。

### 4. 自定义样式

- 修改 `tailwind.config.js` 中的主题颜色
- 在 `app/globals.css` 中添加自定义样式

## 部署到 Vercel

### 方法一：通过 GitHub 部署（推荐）

1. 将代码推送到 GitHub 仓库
2. 访问 [Vercel](https://vercel.com)
3. 点击 "New Project"
4. 导入你的 GitHub 仓库
5. Vercel 会自动检测 Next.js 项目并配置
6. 点击 "Deploy" 完成部署

### 方法二：通过 Vercel CLI

```bash
# 安装 Vercel CLI
npm i -g vercel

# 部署
vercel
```

### 环境变量

如果项目需要环境变量，在 Vercel 项目设置中添加。

## 自定义域名

1. 在 Vercel 项目设置中进入 "Domains"
2. 添加你的自定义域名
3. 按照提示配置 DNS 记录

## 浏览器支持

- Chrome (最新)
- Firefox (最新)
- Safari (最新)
- Edge (最新)

## 许可证

MIT

## 贡献

欢迎提交 Issue 和 Pull Request！
